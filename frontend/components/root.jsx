import React from 'react';
import { Provider } from 'react-redux';
import { HashRouter } from 'react-router-dom';

import App from './app';

const Root = ({ store }) => (
    <div>App should render</div>
);

export default Root;

{/* <HashRouter></HashRouter> */}